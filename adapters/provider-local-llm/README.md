# provider-local-llm

Implement the local LLM provider adapter boundary.

- implement the provider-runtime driver contract
- routing stays in `services/provider-runtime`
- do not import sibling adapters
