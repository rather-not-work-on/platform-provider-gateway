# provider-gateway-composer

Compose the provider runtime with repo-local default adapters.

- own default composition only
- keep routing and request normalization in `provider-runtime`
- keep adapter implementations in `adapters/`
